services:
  - type: web
    name: gradio-demo
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python demo_app.py"
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9

